{
  "name": "question-machine",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "check": "node --check server.mjs && node --check lib/evaluator.mjs && node --check public/app.js",
    "test": "node --test tests/*.test.mjs"
  }
}
